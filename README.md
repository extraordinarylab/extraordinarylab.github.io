# ExtraOrdinaryLab website

Minimal static website for ExtraOrdinaryLab.

## Deploy

Place `index.html`, `styles.css`, and `script.js` in the root of the `extraordinarylab.github.io` repository and enable GitHub Pages from the main branch.
